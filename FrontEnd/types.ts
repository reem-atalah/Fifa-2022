export const enum Gender {
	male = "Male",
	female = "Female",
}

export const enum Role {
	admin = "0",
	manager = "1",
	pendingManager = "2",
	fan = "3",
}

export type Stadium = {
	width: Number;
	height: Number;
};
